@using Microsoft.AspNetCore.Html
@model DotnetTestingWebApp.Models.Product

@{
    ViewData["Title"] = "Produk Page";
}

@await Component.InvokeAsync(
    "PageHeader", 
    new { 
        PageTitle = "Produk Manajemen | Tambah Data", 
        breadcrumb = new List<(string, string, string)>
        {
            ("Home", "Index", "Home"),
            ("Produk", "Index", "Product"),
            ("Tambah Data", "", "")
        }
    }
)
<div class="content">
    <div class="container-fluid">
        <div class="row">
            <div class="col-12">
                <card-layout title="Tambah Produk Baru">
							<form asp-action="Create" method="post" id="formProccess">
                                @await Html.PartialAsync("Components/_FormInput", new ViewDataDictionary(ViewData) { { "Label", "Nama Produk" }, { "Name", "Name" }, { "Attributes", "type=\"text\" placeholder=\"Masukan nama produk\" data-test=\"123\" autofocus=\"true\" autocomplete=\"off\" required" } })
                                @await Html.PartialAsync("Components/_FormInput", new ViewDataDictionary(ViewData) { { "Label", "Harga Produk" }, { "Name", "Price" }, { "Attributes", "type=\"number\" placeholder=\"Masukan harga produk\" autocomplete=\"off\" required" } })

                                @await Html.PartialAsync("Components/_SubmitSave")
                            </form>
                </card-layout>
            </div>
        </div>
    </div>
</div>
@section Scripts {
    <script src="~/helpers/js/form-handler.js"></script>
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
    <script>
    handleFormSubmission({
        formSelector: '#formProccess',
        buttonSelector: '#btnSave'
    });
    </script>
}